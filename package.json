{
  "name": "goldloan-tracker",
  "private": true,
  "description": "Gold Loan Tracker (Google Apps Script). No runtime dependencies; package.json exists only to run the tests.",
  "scripts": {
    "test": "node --test --test-reporter=./tests/helpers/reporter.js",
    "test:raw": "node --test",
    "test:ui": "playwright test",
    "test:all": "npm test && npm run test:ui"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
